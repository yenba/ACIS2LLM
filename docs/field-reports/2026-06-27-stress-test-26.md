# Stress Test Batch 26

**Date:** 2026-06-27
**Type:** Automated edge-case stress test
**Queries tested:** 1
**Status:** Issues found

---

## Results Summary

| # | Query | Category | Difficulty | Status | Answer Quality |
|---|-------|----------|------------|--------|----------------|
| 1 | I'm trying to figure out how often we get serious downpours ... | threshold count | medium | error_recovered | correct |

## Query 1: I'm trying to figure out how often we get serious downpours in the 98101 area during the fall. Over the last decade, how many days in November at KSEA actually recorded more than half an inch of rain? I just want to count the heavy rain days versus the typical drizzle.

**Category:** threshold count
**Difficulty:** medium
**Status:** error_recovered
**Answer Quality:** correct

### Issues

#### Parameter name confusion: 'variable' vs 'parameter'

- **Category:** wrong_api_usage
- **Severity:** medium
- **Description:** The agent reported a TypeError on the first call because it used 'variable' instead of 'parameter' as a keyword argument. This suggests the skill documentation does not clearly specify the correct parameter names for the API functions, particularly `acis2llm.monthly_threshold_counts`. The agent recovered after 1 retry, but this initial failure indicates unclear documentation about the function signature.
- **Suggestion:** Add a clear function signature section to the SKILL.md or a dedicated reference document showing the exact parameter names for each API function. For example: `monthly_threshold_counts(station, parameter, threshold, start_date, end_date, ...)` with explicit type hints and descriptions. Consider adding a 'Common Mistakes' or 'Gotchas' section that explicitly warns: 'Use `parameter` (not `variable`) when calling threshold count functions.'
- **Pointer:** `SKILL.md or references/ - function signature documentation`

### What Worked

- The agent correctly identified `acis2llm.monthly_threshold_counts` as the appropriate function for threshold count queries - this is the right tool for the job
- The agent successfully recovered from the parameter error after one retry, demonstrating the retry mechanism works
- The agent included useful supplementary information: per-year breakdown, data completeness (99.8%), and contextual details (wettest November, heaviest single day)
- The agent used `xmacis2py.analysis.number_of_missing_days` to verify data quality, showing good practice in checking completeness
- The final answer was well-structured and directly addressed the user's question about heavy rain days vs drizzle

---

## Consolidated Issues

| Title | Category | Severity | Suggestion |
|-------|----------|----------|------------|
| Parameter name confusion: 'variable' vs 'parameter' | wrong_api_usage | medium | Add a clear function signature section to the SKILL.md or a dedicated reference ... |
